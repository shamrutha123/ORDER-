projection;
strict ( 2 );
use draft;

define behavior for ZC1_107 alias SalesOrderHdr
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

  use association _salesitem { create; with draft; }
}

define behavior for ZCI_1_107 alias SalesOrderItm
{
  use update;
  use delete;

  use association _salesHeader { with draft; }
}